<%= form_with(model: tran, local: true) do |form| %>
  <% if tran.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tran.errors.count, "error") %> prohibited this tran from being saved:</h2>

      <ul>
      <% tran.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :english %>
    <%= form.text_field :english, id: :tran_english %>
  </div>

  <div class="field">
    <%= form.label :chinese %>
    <%= form.text_field :chinese, id: :tran_chinese %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
